const roles = {
  agency: "agency",
  company: "company",
  worker: "worker",
  companyUser: "company.user",
  admin: "admin",
  payroll: "payroll",
  agencyPersonnel: "agency.personnel"
} as const;

export default roles;
