# ============================================
# Sigook.Web - Vue.js 2 Application
# ============================================
# Project-specific rules
# Generic rules (.NET, IDEs, node_modules) are in the root .gitignore

# Build Output (Sigook.Web specific)
wwwroot/
out/
dist/

# Environment Variables (project-local)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs (npm/yarn specific to this project)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor (macOS specific)
.DS_Store

# Vue CLI cache
.eslintcache

# Legacy cursor rules (no longer used)
cursor.rules
