# Azure Pipeline for SigookApp (Flutter Mobile Application)
# Builds Android APK and iOS IPA for beta releases (staging environment only)

name: SigookApp-$(Date:yyyyMMdd)$(Rev:.r)

# Only trigger when SigookApp files change
trigger:
  branches:
    include:
      - main
      - dev
      - SigookApp
  paths:
    include:
      - SigookApp/**
      - .azure-pipelines/sigookapp-pipeline.yml
      - .azure-pipelines/templates/flutter-setup.yml
    exclude:
      - SigookApp/README.md
      - SigookApp/docs/**
      - SigookApp/**/*.md

# PR trigger - only validate PRs to dev (not to main)
# PRs to main are trusted since dev is the quality gate
pr:
  branches:
    include:
      - dev
  paths:
    include:
      - SigookApp/**
      - .azure-pipelines/sigookapp-pipeline.yml
    exclude:
      - SigookApp/README.md
      - SigookApp/docs/**

# Environment detection variables
variables:
  workingDirectory: '$(System.DefaultWorkingDirectory)/SigookApp'
  isMain: $[in(variables['Build.SourceBranch'], 'refs/heads/main','main')]
  isDev: $[in(variables['Build.SourceBranch'], 'refs/heads/dev','refs/heads/development','dev','development')]
  isSigookApp: $[in(variables['Build.SourceBranch'], 'refs/heads/SigookApp','SigookApp')]
  isPR: $[eq(variables['Build.Reason'], 'PullRequest')]
  flutterVersion: 'stable'

stages:
  # ============================================
  # Stage 1: Validate and Test
  # ============================================
  - stage: Validate_and_Test
    displayName: 'Validate & Test'
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: Flutter_Test
        displayName: 'Flutter Analyze & Test'
        steps:
          - checkout: self
            displayName: 'Checkout Repository'

          - task: Bash@3
            displayName: 'Display Build Information'
            inputs:
              targetType: 'inline'
              script: |
                echo "========================================"
                echo "SigookApp Pipeline - Validate & Test"
                echo "========================================"
                echo "Build Number: $(Build.BuildNumber)"
                echo "Source Branch: $(Build.SourceBranch)"
                echo "Build Reason: $(Build.Reason)"
                echo "Working Directory: $(workingDirectory)"
                echo "========================================"

          # Install Flutter SDK
          - task: FlutterInstall@0
            displayName: 'Install Flutter SDK'
            inputs:
              mode: 'auto'
              channel: '$(flutterVersion)'
              version: 'latest'

          - script: |
              flutter --version
              flutter doctor -v
            displayName: 'Flutter Doctor'

          # Get dependencies
          - script: |
              cd $(workingDirectory)
              flutter pub get
            displayName: 'Flutter Pub Get'

          # Run code generation
          - script: |
              cd $(workingDirectory)
              echo "Running build_runner..."
              flutter pub run build_runner build --delete-conflicting-outputs
              echo "✅ Code generation completed"
            displayName: 'Run Build Runner'

          # Analyze code
          - script: |
              cd $(workingDirectory)
              echo "Running flutter analyze..."
              flutter analyze --no-fatal-infos
              echo "✅ Code analysis passed"
            displayName: 'Flutter Analyze'

          # Run tests
          - script: |
              cd $(workingDirectory)
              echo "Running flutter test..."
              flutter test --coverage --machine > test-results.json || true
              echo "✅ Tests completed"
            displayName: 'Flutter Test'

          # Publish test results (if test output exists)
          - task: Bash@3
            displayName: 'Test Summary'
            inputs:
              targetType: 'inline'
              script: |
                cd $(workingDirectory)
                if [ -f "test-results.json" ]; then
                  echo "Test results generated"
                  cat test-results.json | head -100
                else
                  echo "No test results file found"
                fi
                echo ""
                echo "✅ Validation stage completed!"

  # ============================================
  # Stage 3: Build iOS IPA (Beta)
  # ============================================
  - stage: Build_iOS_Staging
    displayName: 'Build iOS (Staging)'
    dependsOn: Validate_and_Test
    condition: and(succeeded(), eq(variables['isSigookApp'], true), ne(variables['isPR'], true))
    pool:
      vmImage: 'macos-latest'
    variables:
      - group: SigookApp-Staging
      - group: SigookApp-iOS-Signing
    jobs:
      - job: Build_IPA_Staging
        displayName: 'Build Staging IPA'
        steps:
          - checkout: self
            displayName: 'Checkout Repository'

          - task: Bash@3
            displayName: 'Display Build Information'
            inputs:
              targetType: 'inline'
              script: |
                echo "========================================"
                echo "iOS Staging Build"
                echo "========================================"
                echo "Build Number: $(Build.BuildNumber)"
                echo "Source Branch: $(Build.SourceBranch)"
                echo "========================================"

          # Install Apple Certificate
          - task: InstallAppleCertificate@2
            displayName: 'Install Apple Certificate'
            inputs:
              certSecureFile: 'distribution.p12'
              certPwd: '$(P12_PASSWORD)'
              keychain: 'temp'
              deleteCert: true
              setUpPartitionIdACLForPrivateKey: true

          # Install Provisioning Profile
          - task: InstallAppleProvisioningProfile@1
            displayName: 'Install Provisioning Profile'
            inputs:
              provisioningProfileLocation: 'secureFiles'
              provProfileSecureFile: '$(PROVISIONING_PROFILE)'
              removeProfile: true

          # Install Flutter SDK
          - task: FlutterInstall@0
            displayName: 'Install Flutter SDK'
            inputs:
              mode: 'auto'
              channel: '$(flutterVersion)'
              version: 'latest'

          # Get dependencies
          - script: |
              cd $(workingDirectory)
              flutter pub get
            displayName: 'Flutter Pub Get'

          # Run code generation
          - script: |
              cd $(workingDirectory)
              flutter pub run build_runner build --delete-conflicting-outputs
            displayName: 'Run Build Runner'

          # Create placeholder .env files
          - script: |
              cd $(workingDirectory)
              echo "# CI environment - using --dart-define instead" > .env.staging
              echo "# CI environment - using --dart-define instead" > .env.production
              echo "✅ Created placeholder .env files"
            displayName: 'Create Placeholder .env Files'

          # Debug: Show installed signing identities
          - script: |
              echo "=== Code Signing Identities ==="
              security find-identity -v -p codesigning
              echo "==============================="
            displayName: 'Debug: Installed Signing Identities'

          # Generate Flutter build with dart-defines
          - script: |
              cd $(workingDirectory)
              echo "Running Flutter build for iOS (generates Runner.xcarchive)..."
              flutter build ios \
                -t lib/main_staging.dart \
                --release \
                --no-codesign \
                --dart-define=ENVIRONMENT=staging \
                "--dart-define=AUTH_AUTHORITY=$(AUTH_AUTHORITY)" \
                "--dart-define=API_BASE_URL=$(API_BASE_URL)" \
                "--dart-define=CLIENT_ID=$(CLIENT_ID)" \
                "--dart-define=REDIRECT_URI=$(REDIRECT_URI)" \
                "--dart-define=POST_LOGOUT_REDIRECT_URI=$(POST_LOGOUT_REDIRECT_URI)" \
                "--dart-define=SCOPES=$(SCOPES)" \
                "--dart-define=APP_NAME=$(APP_NAME)"
              echo "✅ Flutter iOS build complete (no codesign)"
            displayName: 'Flutter Build iOS (No Codesign)'

          # Archive and sign with Xcode
          - task: Xcode@5
            displayName: 'Archive iOS App'
            inputs:
              actions: 'archive'
              configuration: 'Release'
              sdk: 'iphoneos'
              xcWorkspacePath: '$(workingDirectory)/ios/Runner.xcworkspace'
              scheme: 'Runner'
              packageApp: true
              exportPath: '$(workingDirectory)/output'
              exportOptions: 'plist'
              exportOptionsPlist: '$(workingDirectory)/ios/ExportOptions-Staging.plist'
              signingOption: 'manual'
              args: '-allowProvisioningUpdates'

          # Copy IPA to staging directory
          - task: Bash@3
            displayName: 'Prepare IPA Artifact'
            inputs:
              targetType: 'inline'
              workingDirectory: '$(workingDirectory)'
              script: |
                echo "Current directory: $(pwd)"
                mkdir -p $(Build.ArtifactStagingDirectory)/ios
                
                # Debug: Show output directory structure
                echo "Output directory contents:"
                ls -la output/ || true
                
                # Find and copy IPA (Xcode archive creates IPA in output directory)
                IPA_PATH=$(find output -name "*.ipa" 2>/dev/null | head -1)
                if [ -n "$IPA_PATH" ]; then
                  cp "$IPA_PATH" $(Build.ArtifactStagingDirectory)/ios/sigook-staging-$(Build.BuildId).ipa
                  echo "✅ IPA copied from: $IPA_PATH"
                  echo "✅ IPA artifact: sigook-staging-$(Build.BuildId).ipa"
                else
                  echo "❌ IPA not found in output/"
                  echo "Searching for IPAs:"
                  find . -name "*.ipa" 2>/dev/null || echo "No IPAs found"
                  exit 1
                fi
                
                ls -la $(Build.ArtifactStagingDirectory)/ios/

          # Publish IPA artifact
          - task: PublishBuildArtifacts@1
            displayName: 'Publish Staging IPA'
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)/ios'
              ArtifactName: 'sigookapp-ios-staging'
              publishLocation: 'Container'

          - task: Bash@3
            displayName: 'Build Summary'
            inputs:
              targetType: 'inline'
              script: |
                echo ""
                echo "========================================"
                echo "✅ Staging iOS Build Complete!"
                echo "========================================"
                echo "Artifact: sigookapp-ios-staging"
                echo "IPA: sigook-staging-$(Build.BuildId).ipa"
                echo "Build ID: $(Build.BuildId)"
                echo "========================================"
