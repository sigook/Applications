# Reusable template for .NET SDK installation
# Usage:
#   - template: templates/dotnet-setup.yml
#     parameters:
#       sdkVersion: '8.0.415'

parameters:
  - name: sdkVersion
    type: string
    default: '8.0.415'

steps:
  - task: UseDotNet@2
    displayName: 'Install .NET SDK ${{ parameters.sdkVersion }}'
    inputs:
      packageType: 'sdk'
      version: ${{ parameters.sdkVersion }}
