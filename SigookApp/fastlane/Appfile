# Fastlane Appfile for SigookApp
# This file contains the application-specific configuration for Fastlane

# iOS Configuration
# -----------------
# Staging app identifier (used for TestFlight builds)
app_identifier("com.sigook.beta")

# Production app identifier (for App Store releases)
# app_identifier("com.sigook.app")

# Apple Developer Team ID
team_id("NGXFWU4H7Z")

# Apple ID email (for App Store Connect)
# Note: For CI/CD, we use App Store Connect API instead of Apple ID
# apple_id("your-apple-id@example.com")

# App Store Connect Team ID (if you have multiple teams)
# itc_team_id("123456789")

# Android Configuration
# ---------------------
# Android package name for staging
# json_key_file("path/to/google-play-key.json")  # Set via environment variable in CI/CD
package_name("com.sigook.beta")

# Environment-specific overrides
# For production builds, these can be overridden via command line:
#   fastlane ios beta --env production
# Or by using a separate Appfile.production file

# Note: For CI/CD environments, most credentials are passed via environment variables:
# - APP_STORE_CONNECT_API_KEY_ID
# - APP_STORE_CONNECT_API_ISSUER_ID
# - APP_STORE_CONNECT_API_KEY_CONTENT (base64 encoded)
# - GOOGLE_PLAY_JSON_KEY (base64 encoded)
