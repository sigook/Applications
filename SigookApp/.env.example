# Environment Configuration Template
# Copy this file to .env.staging or .env.production and fill in the values

# Environment identifier (staging or production)
ENVIRONMENT=

# OAuth Configuration
# The IdentityServer base URL (e.g., https://your-identityserver-domain)
AUTH_AUTHORITY=

# OAuth Client ID registered in IdentityServer
CLIENT_ID=

# OAuth Redirect URI for login (e.g., sigookcallback://oauth2redirect)
# Must match the configured RedirectUris in IdentityServer client
# Must also be configured in AndroidManifest.xml as an intent-filter
REDIRECT_URI=

# OAuth Post Logout Redirect URI (e.g., com.sigook:/oauth2logout or sigookcallback://logout)
# Must match the configured PostLogoutRedirectUris in IdentityServer client
# Should also be configured in AndroidManifest.xml as an intent-filter if you want to handle the redirect
POST_LOGOUT_REDIRECT_URI=

# API Configuration
API_BASE_URL=

# OAuth Scopes (comma-separated)
SCOPES=

# App Display Name
APP_NAME=
